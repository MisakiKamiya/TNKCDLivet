<Window x:Class="TNKCDLivet.Views.TNKCDCreate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
        xmlns:vm="clr-namespace:TNKCDLivet.ViewModels"
        Title="TNKCDCreate"  Height="720" Width="740">

    <Window.Background>
        <ImageBrush ImageSource = "../img/emoi_haikei.jpg" Stretch="UniformToFill"/>
    </Window.Background>

    <Window.DataContext>
        <vm:TNKCDCreateViewModel/>
    </Window.DataContext>

    <i:Interaction.Triggers>

        <!--Viewに特別な要件が存在しない限りは、トリガーやアクションの自作にこだわらず積極的にコードビハインドを使いましょう -->
        <!--Viewのコードビハインドは、基本的にView内で完結するロジックとViewModelからのイベントの受信(専用リスナを使用する)に限るとトラブルが少なくなります -->
        <!--Livet1.1からはコードビハインドでViewModelのイベントを受信するためのWeakEventLisnterサポートが追加されています -->

        <!--WindowのContentRenderedイベントのタイミングでViewModelのInitializeメソッドが呼ばれます-->
        <i:EventTrigger EventName="ContentRendered">
            <l:LivetCallMethodAction MethodTarget="{Binding}" MethodName="Initialize"/>
        </i:EventTrigger>

        <!--Windowが閉じたタイミングでViewModelのDisposeメソッドが呼ばれます-->
        <i:EventTrigger EventName="Closed">
            <l:DataContextDisposeAction/>
        </i:EventTrigger>

        <!--WindowのCloseキャンセル処理に対応する場合は、WindowCloseCancelBehaviorの使用を検討してください-->

    </i:Interaction.Triggers>

    <Grid >
        <ComboBox SelectedIndex="0" Margin="145,84,385,0" VerticalAlignment="Top" 
                  RenderTransformOrigin="0.45,0.636" Height="31">
            <ComboBoxItem Content="To" FontSize="20" Height="45" Width="165" />
        </ComboBox>

        <ComboBox SelectedIndex="0" Margin="453,84,78,0" VerticalAlignment="Top" 
                  RenderTransformOrigin="0.45,0.636" Height="31">
            <ComboBoxItem Content="From" FontSize="20" Height="45" Width="165" />
        </ComboBox>

        <DatePicker SelectedDate="{Binding ThanksCard.CreatedDateTime}" Margin="453,138,78,434"/>

        <TextBox HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top"
            Text="Title:" FontSize="20" Height="42" Width="476"  Margin="145,186,0,0" />

        <TextBox HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top"
            Text="Body:" FontSize="20" Height="194" Width="476"  Margin="147,249,0,0" />

        <ComboBox SelectedIndex="0" Margin="147,465,437,0" VerticalAlignment="Top" RenderTransformOrigin="0.45,0.636">
            <ComboBoxItem Content="業務関連度" FontSize="20" Height="45" Width="165" />
        </ComboBox>

        <Button Content="Send" Margin="299,512,238,147" Command="{Binding Command}" />

        <Button Content="Clear" Margin="299,572,238,87" Command="{Binding Command}" />

    </Grid>
</Window>