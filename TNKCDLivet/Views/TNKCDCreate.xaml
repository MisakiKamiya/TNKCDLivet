<Window x:Class="TNKCDLivet.Views.TNKCDCreate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
        xmlns:vm="clr-namespace:TNKCDLivet.ViewModels"
        Title="TNKCDCreate"  Height="720" Width="740">

    <Window.Background>
        <ImageBrush ImageSource = "../img/emoi_haikei.jpg" Stretch="UniformToFill"/>
    </Window.Background>

    <Window.DataContext>
        <vm:TNKCDCreateViewModel/>
    </Window.DataContext>

    <i:Interaction.Triggers>

        <!--Viewに特別な要件が存在しない限りは、トリガーやアクションの自作にこだわらず積極的にコードビハインドを使いましょう -->
        <!--Viewのコードビハインドは、基本的にView内で完結するロジックとViewModelからのイベントの受信(専用リスナを使用する)に限るとトラブルが少なくなります -->
        <!--Livet1.1からはコードビハインドでViewModelのイベントを受信するためのWeakEventLisnterサポートが追加されています -->

        <!--WindowのContentRenderedイベントのタイミングでViewModelのInitializeメソッドが呼ばれます-->
        <i:EventTrigger EventName="ContentRendered">
            <l:LivetCallMethodAction MethodTarget="{Binding}" MethodName="Initialize"/>
        </i:EventTrigger>

        <!--Windowが閉じたタイミングでViewModelのDisposeメソッドが呼ばれます-->
        <i:EventTrigger EventName="Closed">
            <l:DataContextDisposeAction/>
        </i:EventTrigger>

        <!--WindowのCloseキャンセル処理に対応する場合は、WindowCloseCancelBehaviorの使用を検討してください-->

    </i:Interaction.Triggers>

    <Grid >


        <ComboBox  ItemsSource="{Binding Employee}" DisplayMemberPath="Name" 
                       FontSize="15" Height="30" Width="165" Margin="145,85,385,0" VerticalAlignment="Top" RenderTransformOrigin="0.45,0.636"/>

        <ComboBox  ItemsSource="{Binding Employee}" DisplayMemberPath="Name" 
                       FontSize="15" Height="30" Width="165" Margin="455,85,80,0" VerticalAlignment="Top" RenderTransformOrigin="0.45,0.636"/>

        <DatePicker SelectedDate="{Binding ThanksCard.CreatedDateTime}" Margin="455,145,80,500"/>

        <TextBox HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top"
            Text="Title:" FontSize="15" Height="42" Width="476"  Margin="145,185,0,0" />

        <TextBox HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top"
            Text="Body:" FontSize="15" Height="194" Width="476"  Margin="145,250,0,0" />

        <ComboBox  ItemsSource="{Binding Work}" DisplayMemberPath="WorkRelation" 
                       FontSize="15" Height="25" Margin="145,465,415,0" VerticalAlignment="Top" RenderTransformOrigin="0.45,0.636"/>

        <Button Content="Send" Margin="300,510,240,150" Command="{Binding Command}" />

        <Button Content="Clear" Margin="300,570,240,90" Command="{Binding Command}" />

    </Grid>
</Window>